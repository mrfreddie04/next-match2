import React from 'react';
import ResetPasswordForm from './ResetPasswordForm';

export default async function ResetPasswordPage() {
  ///const result = await resetPassword(searchParams.token);

  return (
    <ResetPasswordForm />
  )
}